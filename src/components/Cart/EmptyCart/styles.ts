import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  emptyCartContainer: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
  },
  emptyCartText: {
    fontSize: 36,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
